import { Metadata } from "next";

export const siteConfig: Metadata = {
  title: {
    default: "Auto Heal",
    template: "Auto Heal | %s",
  },
  description: "Auto-heal is a home care app for all",
};
